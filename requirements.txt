python-telegram-bot>=20.0
python-docx
aiosqlite
python-dotenv
pytz
aiofiles
motor>=3.5
pymongo>=4.0

# PDF conversion requires LibreOffice installed on the system:
#   Linux (Render):  sudo apt-get install -y libreoffice-writer
#   macOS:           brew install libreoffice
#   Windows:         winget install TheDocumentFoundation.LibreOffice
# Set PDF_ENABLED=false to disable PDF generation without uninstalling.
# Set SOFFICE_PATH=/usr/bin/soffice to override the binary path.
